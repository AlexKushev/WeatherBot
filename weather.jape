Phase: WeatherPhase
Input: Token Lookup
Options: control = appelt

Rule: WeatherCondition
(
 (
  {Token.root == "rain"} | {Token.root == "snow"} 
 )
 |
 {Lookup.majorType == "weather"}
):match
-->:match.WeatherCondition = {rule = "WeatherCondition" string = :match.Token.root}
