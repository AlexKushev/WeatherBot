Phase:firstphase
Input: Lookup Token
Options: control = appelt

Rule: WeatherCategory
(
{Token.root == "rain"} | {Lookup.majorType == "weather"}
): match
-->
:match.Weather = {rule= "WeatherCategory" string=:match.Token.root }
